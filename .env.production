# Production environment - Backend API URL for Vercel
# For serverless function in this repo we expose /api/users
# Set to a full external API if you have one (recommended for persistence)
VITE_API_URL=/api/users
